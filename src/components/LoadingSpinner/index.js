import React from 'react';

import * as S from './styled';

const LoadingSpinner = () => {
  return (
    <S.SpinnerWrapper>
      <S.Spinner />
    </S.SpinnerWrapper>
  );
};

export default LoadingSpinner;
