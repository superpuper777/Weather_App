import React from 'react';
import SearchBar from './SearchBar';
import WeatherInfo from './WeatherInfo';

const Home = () => {
  return (
    <div>
      <SearchBar />
      <WeatherInfo />
    </div>
  );
};

export default Home;
